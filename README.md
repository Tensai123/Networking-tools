# Networking tools
Here's gonna be my tools to simplify tasks within networks.
TCP - directory with typical TCP tools
UDP - directory with typical TCP tools
Netcat.py:
Exactly the same tool like netcat. Type Netcat.py --help for more info.

